apiVersion: v1
kind: Service
metadata:
  name: heart-api-service
spec:
  type: LoadBalancer
  selector:
    app: heart-api
  ports:
    - protocol: TCP
      port: 80        # external port
      targetPort: 8000 # matches containerPort
